import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Avinash Dangi — Lead Software Engineer",
  description:
    "I build complete AI-native products — from database schema to PWA to CI/CD pipeline. 9 years across fintech, edtech, and AI.",
  openGraph: {
    title: "Avinash Dangi — Lead Software Engineer",
    description:
      "I build complete AI-native products — from database schema to PWA to CI/CD pipeline.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable}`}
    >
      <body className="antialiased">{children}</body>
    </html>
  );
}
